def linear_search(list, target):
  """
  Returns the index (numerical) of target if within list of elements, else return -1
  """
  for i in range(0, len(list):
    if list[i] == target:
  return -1

try:
  if __main__ == "__name__":
    numbers = [1, 3, 5, 7, 9, 10]
    target = 9

    print(linear(numbers, target))
except:
  return -1
